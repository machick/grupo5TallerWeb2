import {ITheme} from './itheme.interface';
export const THEMES: ITheme = {
    default: {
        primaryColor: '#FFFFFF',
        textColor: '#000000'
    },
    dark: {
        primaryColor: 'grey',
        textColor: '#FFFFFF'
    }
}