// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: true,
  API:'https://birraapp-api-2pile.ondigitalocean.app',
  API_WHITELIST:['birraapp-api-2pile.ondigitalocean.app'],
  API_URL_IMAGE: 'https://birraapp-api-2pile.ondigitalocean.app',
  FILE_SIZE: 5000000,
  FILE_SIZE_TEXT: 5,
  //API_WHITELIST: ['localhost:3000', 'localhost:3001', 'localhost:3002'],
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
